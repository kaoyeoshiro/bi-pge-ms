# Configuração do banco de dados (desenvolvimento local)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui
DB_NAME=pge_bi

# OU use DATABASE_URL diretamente (Railway fornece automaticamente)
# DATABASE_URL=postgresql://user:pass@host:port/dbname

# Senha de acesso ao painel administrativo
ADMIN_PASSWORD=sua_senha_admin

# Origens permitidas para CORS (separar por vírgula)
# CORS_ORIGINS=["http://localhost:5173","https://seu-dominio.up.railway.app"]
